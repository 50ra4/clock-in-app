export const FETCH_STATUS_ENUM = {
  initialized: 'INITIALIZED',
  fetching: 'FETCHING',
  fetched: 'FETCHED',
} as const;

export const LOGIN_STATUS_ENUM = {
  initialized: 'INITIALIZED',
  success: 'SUCCESS',
  failed: 'FAILED',
} as const;
