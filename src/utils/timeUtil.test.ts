import { timeRangeToMinute } from './timeUtil';

describe('timeUtil', () => {
  describe('timeRangeToMinute', () => {
    it.todo('should return Number.Nan if time range is empty');
    it.todo('should return minute');
    it.todo('should return value added for 24 hours is added if the start hour is greater than the end hour');
  });
});
